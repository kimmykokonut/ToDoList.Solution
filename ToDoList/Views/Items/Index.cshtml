@{
  Layout = "_Layout";
}

@using ToDoList.Models;

<h1>Items</h1>
<ul>
  @foreach(Item item in Model)
  {
    <li>@Html.ActionLink($"{item.Description}", "Details", new { id = item.ItemId })</li>
    //will route to Details action in ItemsController
  }
</ul>
@* @if (@Model.Count == 0)
{
  <h3>No items have been added yet!</h3>
}

@foreach (Item item in Model)
{
  <li>@item.Description</li>
} *@
<p>@Html.ActionLink("Add new item", "Create")</p>
<p>@Html.ActionLink("See all categories", "Index", "Categories")</p>
<p>@Html.ActionLink("go home", "Index", "Home")</p>